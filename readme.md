# EnigmaApl - Enigma for Apple

This project, which is still in a premature phase, will contain an astrology application.
I will build this application in parallel for macOS, iPadOS and for Windows.
The Windows project is in the repository EnigmaWin.
Part of the functionality will be retrieved from the existing repository EnigmaSuite.

The project uses Swift and SwiftUI.

